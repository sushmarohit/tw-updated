# Docker Postgres (see DOCKER_POSTGRES_LOCAL_SETUP.md): run `docker compose up -d postgres` then use:
DATABASE_URL="postgresql://twelfthkey:twelfthkey_password@localhost:5433/twelfthkey"
DIRECT_URL="postgresql://twelfthkey:twelfthkey_password@localhost:5433/twelfthkey"

# Optional: where to receive contact form submissions (defaults to EMAIL_FROM or SMTP_USER)
CONTACT_EMAIL=
# Supabase (optional when using Docker Postgres)
NEXT_PUBLIC_SUPABASE_URL=
HUBSPOT_API_KEY=badcf
NEXT_PUBLIC_GA4_MEASUREMENT_ID=mjbds


# email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tw@gmail.com
SMTP_PASSWORD=cdji jsnz rpsm ltrm
EMAIL_FROM=tw@gmail.com
EMAIL_FROM_NAME=TwelfthKey Consulting

# Google Calendar: create an Appointment schedule in Google Calendar, copy the booking/scheduling link
NEXT_PUBLIC_GOOGLE_CALENDAR_BOOKING_URL=